.header
  -# = render partial: '/template/header'
  -# ヘッダー、フッターの記述はマージ後に解除予定

.wrapper
  %main.mypage
    =render "shared/mypage_contents"
    =render "shared/mypage_menu"

.footer
  -# = render partial: '/template/footer'
  -# ヘッダー、フッターの記述はマージ後に解除予定
